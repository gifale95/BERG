# Pycortex ROI labels

This code computes the geodesic distances between vertices in `fsaverage` surfaces space. These geodesic distances are used to estimate the voxels neighborhoods for the RSA searchlight analyses.